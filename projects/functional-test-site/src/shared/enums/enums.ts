/* 3rd party libraries */

/* locally accessible feature module code, always use a relative path */

export enum LogLevel {
  info,
  success,
  warning,
  error
}

export enum Season {
  spring = 'S',
  summer = 'M',
  autumn = 'A',
  winter = 'W'
}

export enum Month {
  jan,
  feb,
  mar,
  apr,
  may,
  jun,
  jul,
  aug,
  sep,
  oct,
  nov,
  dec
}
