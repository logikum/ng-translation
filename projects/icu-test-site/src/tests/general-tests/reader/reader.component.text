#
# Section *** [folder/]file[.node[.node]...]
# ------------------------------------------
> general.reader
#
# Texts *** [node.[node.]...]keyword: text
# ------------------------------------------

title:  Using Structural Directive (Reader)

form.hint:  To add a new product, enter the following information
form.name.label:  Product name
form.name.minLength:  The product name must be at least { minLength, number } characters,
                      but { recommended, number } characters long is recommended.
form.name.maxLength:  The product name cannot exceed { maxLength, number } characters.
