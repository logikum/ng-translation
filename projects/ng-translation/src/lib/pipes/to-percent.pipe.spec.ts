import { ToPercentPipe } from './to-percent.pipe';

describe('ToPercentPipe', () => {
  it('create an instance', () => {
    const pipe = new ToPercentPipe();
    expect(pipe).toBeTruthy();
  });
});
