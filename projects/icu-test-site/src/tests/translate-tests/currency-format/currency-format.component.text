#
# Section *** [folder/]file[.node[.node]...]
# ------------------------------------------
> translate.currency
#
# Texts *** [node.[node.]...]keyword: text
# ------------------------------------------

title: Currency Formatting

default_f: Default currency format
default_l: { price, number, ::currency/___ }
default_s: { price, number, ::currency/___ }

code_f: Currency code format
code_l: { price, number, ::currency/___ currency-code}
code_s: { price, number, ::currency/___ code }

name_f: Currency name format
name_l: { price, number, ::currency/___ currency-name}
name_s: { price, number, ::currency/___ name }

symbol_f: Currency symbol format
symbol_l: { price, number, ::currency/___ currency-symbol}
symbol_s: { price, number, ::currency/___ symbol }
